{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "skeleton",
  "title": "Skeleton",
  "description": "Redaction placeholder, like .redacted(reason: .placeholder).",
  "registryDependencies": [
    "utils"
  ],
  "files": [
    {
      "path": "registry/apple-ui/skeleton.tsx",
      "content": "import * as React from \"react\";\n\nimport { cn } from \"@/lib/utils\";\n\n/** Redaction placeholder — the .redacted(reason: .placeholder) look. */\nfunction Skeleton({ className, ...props }: React.ComponentProps<\"div\">) {\n  return (\n    <div\n      data-slot=\"skeleton\"\n      className={cn(\n        \"animate-pulse rounded-[6px] bg-fill-tertiary\",\n        className\n      )}\n      {...props}\n    />\n  );\n}\n\nexport { Skeleton };\n",
      "type": "registry:ui"
    }
  ],
  "type": "registry:ui"
}